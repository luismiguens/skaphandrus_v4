{# empty Twig template #}
<url>
    <loc>http://skaphandrus.com{{ path('species', {'slug':slugify(specie.name), 'locale': 'en'}) }}</loc>
    <lastmod>{{ "now"|date("Y-m-d") }}</lastmod>  
    <changefreq>monthly</changefreq>
    <priority>0.8</priority>
    <xhtml:link 
        rel="alternate"
        hreflang="en"
        href="http://skaphandrus.com{{ path('species', {'slug':slugify(specie.name), 'locale': 'en'}) }}"
        />
    <xhtml:link 
        rel="alternate"
        hreflang="pt"
        href="http://skaphandrus.com{{ path('species', {'slug':slugify(specie.name), 'locale': 'pt'}) }}"
        />
</url>
