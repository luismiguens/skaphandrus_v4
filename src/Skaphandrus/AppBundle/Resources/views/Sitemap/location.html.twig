{# empty Twig template #}
<url>
    <loc>http://skaphandrus.com{{ path('location', {'country':slugify(location.region.country), 'slug':slugify(location), 'locale': 'en'}) }}</loc>
    <lastmod>{{ "now"|date("Y-m-d") }}</lastmod>  
    <changefreq>monthly</changefreq>
    <priority>0.8</priority>
    <xhtml:link 
        rel="alternate"
        hreflang="en"
        href="http://skaphandrus.com{{ path('location', {'country':slugify(location.region.country), 'slug':slugify(location), 'locale': 'en'}) }}"
        />
    <xhtml:link 
        rel="alternate"
        hreflang="pt"
        href="http://skaphandrus.com{{ path('location', {'country':slugify(location.region.country.getCountryName('pt')), 'slug':slugify(location), 'locale': 'pt'}) }}"
        />
</url>
