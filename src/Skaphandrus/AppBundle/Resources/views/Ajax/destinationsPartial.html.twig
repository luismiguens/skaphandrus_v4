{% if map %}
    <script type="text/javascript">
        google.maps.event.trigger(window, 'resize', {});
        setTimeout(function () {
        {{ map.javascriptVariable }}.setCenter({{ map.center.javascriptVariable }});
            }, 100);
    </script>

    {{ google_map(map) }}
{% else %}
    {{ "page.destinations.label.no_map"|trans }}
{% endif %}
