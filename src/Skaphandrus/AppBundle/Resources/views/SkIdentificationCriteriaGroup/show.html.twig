{% extends '::base.html.twig' %}

{% block body -%}
    <h1>SkIdentificationCriteriaGroup</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul class="record_actions">
        <li>
            <a href="{{ path('sk_identification_criteria_group') }}">
                Back to the list
            </a>
        </li>
        <li>
            <a href="{{ path('sk_identification_criteria_group_edit', { 'id': entity.id }) }}">
                Edit
            </a>
        </li>
        <li>{{ form(delete_form) }}</li>
    </ul>
{% endblock %}
