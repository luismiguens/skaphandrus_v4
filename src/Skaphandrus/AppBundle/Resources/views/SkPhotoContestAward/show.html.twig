{% extends '::base.html.twig' %}

{% block body -%}
    <h1>SkPhotoContestAward</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Image</th>
                <td>{{ entity.image }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('photo_contest_award_admin') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('photo_contest_award_admin_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
