{% extends '@SkaphandrusAppBundle/Resources/views/content_layout.html.twig' %}

{% block body %}
    <h2>{{ "sponsors"|trans }}: {{ contest.name }}</h2>
    {% for sponsor in contest.sponsors %}
        <p>{{ sponsor.name }}<p/>
    {% endfor %}
{% endblock %}
