{% extends '::base.html.twig' %}

{% block body -%}
    <h1>SkPhotoSpeciesValidation</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Rating</th>
                <td>{{ entity.rating }}</td>
            </tr>
            <tr>
                <th>Createdat</th>
                <td>{{ entity.createdAt|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('photo_species_validation_admin') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('photo_species_validation_admin_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
