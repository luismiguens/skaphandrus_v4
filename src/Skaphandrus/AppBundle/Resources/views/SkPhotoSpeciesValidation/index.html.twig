{% extends '::base.html.twig' %}

{% block body -%}
    <h1>SkPhotoSpeciesValidation list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Rating</th>
                <th>Createdat</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('photo_species_validation_admin_show', { 'id': entity.id }) }}">{{ entity.rating }}</a></td>
                <td>{% if entity.createdAt %}{{ entity.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('photo_species_validation_admin_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('photo_species_validation_admin_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('photo_species_validation_admin_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
