{% extends '::base.html.twig' %}

{% block body -%}
    <h1>SkPhotoContestCategoryJudgeVotation edit</h1>

    {#{{ form(edit_form) }}#}


    {{ form_start(edit_form, { 'attr': {'class': 'form-horizontal'} }) }}

    <table>
        <thead>
            <tr>
                <th>Image</th>
                <th>Pontuation</th>
            </tr>
        </thead>

        {% for photo_points in edit_form.judgeVote %}
            {% set photo = photo_points.vars.data.photo %}
            <tr>
                <td> 
                    <a href="{{path('photo_no_slug', { 'id': photo.id })}}" > 
                        <img src="{{ photo.getWebPath | imagine_filter('sk_widen_240') }}" class="feed-photo" alt="{{ photo }}">
                    </a>
                </td>
                <td>
                    {{ form_widget(photo_points.points) }}
                </td>
            </tr>
        {% endfor %}
    </table>

        {{ form_end(edit_form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('judge_votation_admin', {'contest_id':contest.id}) }}">
                Back to the list
            </a>
        </li>
        <li>{{ form(delete_form) }}</li>
    </ul>

    {# <button type="submit" class="btn btn-primary">{{ "form.common.btn.update"|trans }}</button>#}


{% endblock %}
