{% extends '::base.html.twig' %}

{% block body -%}
    <h1>SkIdentificationCriteria</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Orderby</th>
                <td>{{ entity.orderBy }}</td>
            </tr>
            <tr>
                <th>Iscumulative</th>
                <td>{{ entity.isCumulative }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('identification_criteria_admin') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('identification_criteria_admin_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
